const numberToWords = require('../02_numbers_in_words');

describe('FizzBuzz', function () {
    // it('should return "seven hundred and fourty five dollars"', () => {
    //     const output = numberToWords(745);
    //     expect(output).toEqual('seven hundred and fourty five dollars');
    // });
    // it('should return "forty-two dollars"', () => {
    //     const output = numberToWords(42);
    //     expect(output).toEqual('forty-two dollars');
    // });
    // it('should return "one dollar"', () => {
    //     const output = numberToWords(1);
    //     expect(output).toEqual('one dollar');
    // });
    // it('should return "thirteen dollars"', () => {
    //     const output = numberToWords(13);
    //     expect(output).toEqual('thirteen dollars');
    // });
    // Define more test cases, like: decimals, numbers up to trillions
});